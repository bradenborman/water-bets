import moment from "moment-timezone";

// //Gets the time in users timezone 3 days from now - will need to be put back into UTC for storing
// export const threeHoursFromNow = (): string => {
//   return moment()
//     .tz(Intl.DateTimeFormat().resolvedOptions().timeZone)
//     .add(3, "hours")
//     .format("YYYY-MM-DDTHH:mm");
// };

// export const rightNow = (): string => {
//   return moment().format("YYYY-MM-DDTHH:mm");
// };

// export const weekFromNow = (): string => {
//   return moment()
//     .add(1, "week")
//     .format("YYYY-MM-DDTHH:mm");
// };
